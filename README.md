## bbkf-pandoc

A Pandoc writer (in Lua) for the flavor of BBCode used by the Kiwi Farms.

### Usage

Download the `bbkf.lua` file and stick it somewhere accessible.

Write in your preferred (Pandoc-supported) format and then run:

```sh
pandoc INPUT_FILE -t /path/to/bbkf.lua > OUTPUT_FILE
```
